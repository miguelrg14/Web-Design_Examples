


/*
    var cosas = new Array("Botella", "Zeta", "Androide", "Minuto");

    cosas.sort();
    */
    /*  cosas.reverse();  */
    /*
    cosas.shift();
    cosas.pop();

    for (var i = 0 ; i < cosas.length ; i++)
    {
        document.write("Cosa Nº" + i + " = " + cosas[i] + "<br>");
    }

*/

function recorrer()
{
    var meses = new Array("Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio", "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre");
    
    for (var i = 0; i < meses.length; i++)
    {
        document.write("Mes Nº " + i + " : " + meses[i] + "<br>");
    }
}

function Enviar()
{
    if(formulario.rojo = "ro")
    {
        document.write("Google");
    }

    else if(formulario.azul = "az")
    {
        document.write("Marca");
    }
}